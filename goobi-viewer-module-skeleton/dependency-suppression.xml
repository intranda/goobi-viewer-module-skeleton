<?xml version="1.0" encoding="UTF-8"?>
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">

    <!-- Suppressions for this module's own dependencies.

         viewer-core and its whole transitive tree are excluded from the scan in the pom, so
         findings on artifacts the core brings in do not appear here; they are analysed and
         suppressed in the core repository's own dependency-suppression.xml. What this file
         covers is only what this module adds or resolves to a different version than the
         core does, which keeps the same argument from living in three places. -->

</suppressions>
